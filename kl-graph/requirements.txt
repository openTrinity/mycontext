# Core
qdrant-client>=1.9.0
zvec>=0.6.0
aiosqlite>=0.19.0
litellm>=1.0,<1.90
jieba>=0.42
numpy>=2.0
pydantic>=2.0
omegaconf>=2.3

# Graph storage (default backend)
ladybug>=0.18.0      # Native Cypher traversal + hybrid SQLite content store

# Utilities
tqdm>=4.60
nltk>=3.9
httpx>=0.27
tiktoken>=0.7        # token-budget chunking (naive_merge), cl100k_base
filelock>=3.0        # cross-platform process lock for concurrent-ingest guard

# Windows only: system timezone database is absent, required by ZoneInfo
tzdata; sys_platform == "win32"

# Optional: alternative graph DB backend (install as needed)
# falkordblite>=0.2.0     # GraphBLAS Cypher engine (SSPL, subprocess)
