"""LongMemEval conversion and evaluation helpers."""

# Keep package initialization side-effect free so
# ``python -m kl_graph.evaluation.longmemeval.convert`` does not import the
# target module before runpy executes it.
