engine-strict=true
save-exact=true

# pnpm：不做依赖提升，未在 package.json 声明的包一律不可见。
# 「幽灵依赖」因此会在本机就暴露，而不是等到别人的机器或 CI 上才炸。
hoist=false

# electron-rebuild 需要能解析到 node_modules 里的 native 模块，
# 这类工具对 pnpm 的软链布局不完全兼容，故为其放宽解析。
public-hoist-pattern[]=*electron*
public-hoist-pattern[]=*node-gyp*
